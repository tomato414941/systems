[project]
name = "systems"
version = "0.1.0"
description = "ALife simulation - AI agents with minimal physics, emergent rules"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
